# 04. Common Kubectl Commands

* The `Kubectl` command-line utility is used to create objects and interact with the Kubernetes API

## Namespaces

* Namespaces are used to organize objects in the cluster
